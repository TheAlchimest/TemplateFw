@using TemplateFw.Dtos
@inject IOptions<WebSettings> webSettings
@model PagedList<PaymentInfoDto>
@{
    int itemNo = Model.FirstItemNo;
}
<div class="form-head mb-sm-5 mb-3 d-flex flex-wrap align-items-center">
    <div class=" mr-auto">
        <h3 class="font-w600 mb-2">@modulesLocalizer["Payments"]</h3>
        <p class="mb-0">@modulesLocalizer["Payment_Management"]</p>
    </div>
    <a href="/#!/payment/add" class="btn btn-outline-primary mr-3">
        <i class="fas fa-layer-plus fa-lg mr-2"></i> اضافة 
    </a>
</div>

@Html.Partial("/Views/Payment/IndexFilter.cshtml",new PaymentFilter())

<div class="card">
    <div style="visibility:hidden" class="loader-box">جاري التحميل ...</div>
    <div class="card-body" id="containerBox">
        @Html.Partial("/Views/payment/IndexContent.cshtml", Model)
    </div>
</div>
